# 规范化受管理的历史实体

地区、地点、历史时期、历史人物和主题标签使用不可变标识建模，并通过多对多关联连接历史事件；一个事件可以关联多个地点，主分类保持六个固定值。规范实体允许同名并使用消歧名称区分，编辑者可以创建，只有管理员可以修改、合并或停用；名称不保留多版本，所有事件统一显示当前名称。历史事件使用不可变 UUIDv7 作为身份、可修改 slug 作为可读别名，API、筛选条件和分享链接均使用稳定标识，以承担更多关联表写入的成本换取数据去重、语境关系完整性和永久链接稳定性。
