# 使用 SVG 与 HTML 构建可访问时间线

时间线优先采用自研的 SVG/HTML 混合结构，而不引入完整时间线库或直接使用纯 Canvas：SVG 表达连续轴线、刻度和事件区间，HTML 提供可聚焦的事件内容。该选择牺牲部分开箱即用能力，以换取历史时间模型、聚合规则、响应式布局和键盘体验的完整控制，并避免绑定第三方时间线库的数据模型与样式体系。若万级事件压测无法满足交互指标，则改为 Canvas 绘制轴线、事件簇及非活动事件，同时保留 HTML 可访问交互层。
