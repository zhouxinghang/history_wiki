# 通过不可变事件版本发布内容

编辑者录入和修改草稿，管理员负责发布与下线；每个事件最多有一个活动草稿，草稿修改期间普通读者仍看到最近的已发布版本。每次发布形成不可变事件版本，恢复旧版本只会生成新草稿；已下线事件不出现在任何公开接口中，但可从最后发布版本建立草稿并再次发布。批量导入只创建草稿且不得更新现有事件。更新使用乐观锁且历史事件不物理删除，以承担额外存储与冲突处理成本换取审计、恢复能力和已发布内容的可追踪性。
